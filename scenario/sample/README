Stat the node server:
	./START

Start the AB processs through AB server:
	./STARTAB <AB file name> <number of process>

Send AB requests:
	./TESTAB <number of requests> <level of concurrency>

Stop AB processes:
	./STOPAB
